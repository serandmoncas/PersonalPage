# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

This is a personal page and portfolio project. The repository is in its early stages — no framework or tooling has been committed yet. Before adding code, confirm the intended stack with the user.

## When Starting Work

Since no framework has been chosen yet, ask the user before assuming a tech stack. Common choices for personal portfolios include Next.js, Astro, or plain HTML/CSS. The chosen stack will determine build, dev, and lint commands.

## Once Stack Is Established

Update this file with:
- Dev server command (e.g., `npm run dev`)
- Build command (e.g., `npm run build`)
- Lint/format command (e.g., `npm run lint`)
- Test command if applicable
- Deployment target and how to deploy (e.g., Vercel, GitHub Pages)
